1. a think something simple like the chat ui, it is mainly for me to communicate with the database (add info) and test the chat communication with the database and seeing that things are actually happening as expected
2. yes just front end that talks with the scheduling api
3. what you wrote plus: maybe a search bar to find info, something like sql questions